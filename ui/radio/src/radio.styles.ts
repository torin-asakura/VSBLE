import { createBaseStyles }       from './styles'
import { createAppearanceStyles } from './styles'
import { createShapeStyles }      from './styles'

export const baseStyles = createBaseStyles()
export const appearanceStyles = createAppearanceStyles()
export const shapeStyles = createShapeStyles()
